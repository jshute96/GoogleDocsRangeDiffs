// Background service worker for the GoogleDocsDiffRange extension.
// TODO: Add extension logic here.

chrome.action.onClicked.addListener(async (_tab) => {
  console.log('Extension icon clicked');
});
