# TODO

* Make less ugly icons
* Add e2e tests using the existing Playwright fixture (`tests/fixtures/extension.ts`)
* Test with the real extension loaded (all testing so far was via Playwright `page.evaluate` injection, not with the actual extension)
